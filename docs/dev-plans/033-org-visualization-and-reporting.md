# DEV-PLAN-033：Org 可视化与高级报告

**状态**: 规划中（2025-01-15 12:00 UTC）

## 背景
- 对应 020 步骤 13，需提供组织图导出、节点/人员路径查询及高级报表能力。

## 目标
- 组织图导出（JSON/SVG/PNG 任一实现）可用。
- 节点/人员路径查询接口可用，支持租户隔离。
- 高级报表输出与性能基线对齐，提供 `org_reporting` 视图/快照（含 as_of_date 索引）供 BI 使用。

## 实施步骤
1. [ ] 实现组织图导出接口（至少一种格式），涵盖有效期与租户隔离。
2. [ ] 实现节点/人员路径查询 API，复用闭包表/快照以避免递归瓶颈。
3. [ ] 设计/实现高级报表（路径拍平等），提供 `org_reporting` 视图/快照并评估性能与缓存策略。
4. [ ] 补充测试与文档，说明接口契约与限制。

## 交付物
- 可视化导出与路径查询实现、测试。
- 报表能力与说明。
