# DEV-PLAN-032：Org 权限映射与业务关联

**状态**: 规划中（2025-01-15 12:00 UTC）

## 背景
- 对应 020 步骤 12，需推进组织节点与安全组映射、权限预览，以及与项目/成本中心等业务对象的关联；涉及 finance 冻结目录前必须先解冻并更新 AGENTS。

## 目标
- 提供组织节点 ↔ 安全组映射与权限预览接口（只读/预览态）。
- 在不违反冻结政策下，设计业务对象关联表/接口。
- 保留 feature flag 与回滚路径。

## 实施步骤
1. [ ] 设计并实现组织节点与安全组映射模型/接口（预览态），受 feature flag 控制。
2. [ ] 设计业务对象关联（项目/成本中心等）方案，若触及 finance 目录，先在 dev-plan 解除冻结并更新 AGENTS。
3. [ ] 提供权限预览接口与最小测试覆盖，确保租户隔离与鉴权。
4. [ ] 文档化特性开关、回滚与跨模块协作约束。

## 交付物
- 权限映射/预览接口与测试。
- 业务关联设计与实施（符合冻结策略）。
